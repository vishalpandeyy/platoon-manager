var admin = require('../api/shared/cloud-messaging')

var message = require('../api/shared/messsages')

describe('Firebase cloud messaging', () => {
    
    describe('Send notification to one vehicle', () => {
        it('should return failureCount = 0')
    })

    describe('Send notification to all platoon vehicles on platoon update', () => {
        it('should return failureCount = 0')
    })

    describe('Sent failure alert to all platooon vehicles on failure', () => {
        it('should return failureCount = 0')
    })

});